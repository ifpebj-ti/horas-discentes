name: Automatic Release CI/CD

on:
  pull_request:
    types: [closed]
    branches: ["main"]

permissions:
  contents: write
  packages: write

jobs:
  prepare-tag:
    name: 🏷️ Preparar nova tag
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    outputs:
      new_tag: ${{ steps.nova_tag.outputs.nova_tag }}
      tipo:    ${{ steps.tipo.outputs.tipo }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - id: tipo
        name: Extrair tipo de mudança da PR
        run: |
          echo "Conteúdo do corpo PR:"
          echo "${{ github.event.pull_request.body }}"

          PR_BODY="${{ github.event.pull_request.body }}"
          echo "$PR_BODY" > pr_body.txt

         tipo_encontrado=""
          if grep -q "^\s*-\s*\[x\]\s*marco-no-projeto" pr_body.txt; then
            tipo_encontrado="marco-no-projeto"
          elif grep -q "^\s*-\s*\[x\]\s*nova-feature" pr_body.txt; then
            tipo_encontrado="nova-feature"
          elif grep -q "^\s*-\s*\[x\]\s*bug-fix" pr_body.txt; then
            tipo_encontrado="bug-fix"
          fi

          if [ -z "$tipo_encontrado" ]; then 
            echo "Erro: Nenhum tipo de mudança foi selecionado ou a detecção falhou."
            echo "tipo=default-patch" >> "$GITHUB_OUTPUT" 
            
          else
            echo "Tipo de mudança detectado: $tipo_encontrado"
            echo "tipo=$tipo_encontrado" >> "$GITHUB_OUTPUT"
          fi
        shell: bash

      - id: ultima
        name: Obter a última tag da release
        run: |
          tag=$(git describe --tags $(git rev-list --tags --max-count=1) 2>/dev/null || echo "0.0.0")
          echo "tag_atual=$tag" >> "$GITHUB_OUTPUT"
          echo "Última tag encontrada: $tag" 

      - id: nova_tag
        name: Calcular nova tag (SemVer)
        run: |
          tag_atual="${{ steps.ultima.outputs.tag_atual }}"
          tipo="${{ steps.tipo.outputs.tipo }}"

          IFS='.' read -r major minor patch <<< "$tag_atual"
          
          echo "Calculando nova tag com tag_atual: $tag_atual e tipo: $tipo" # Adicionado para depuração

          case "$tipo" in
            bug-fix)           patch=$((patch + 1));;
            nova-feature)      minor=$((minor + 1)); patch=0;;
            marco-no-projeto)  major=$((major + 1)); minor=0; patch=0;;
            default-patch)     patch=$((patch + 1));; # Caso o tipo não seja encontrado
            *)                 echo "Aviso: Tipo de mudança desconhecido ou não detectado. Incrementando patch por padrão."; patch=$((patch + 1));;
          esac
          
          nova_tag="${major}.${minor}.${patch}"
          echo "nova_tag=$nova_tag" >> "$GITHUB_OUTPUT"
          echo "Nova tag calculada: $nova_tag" 

  frontend:
    name: 🏗️ Frontend CI/CD
    needs: prepare-tag
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Docker Login
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Frontend Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./front
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/horas-discentes-frontend:${{ needs.prepare-tag.outputs.new_tag }}
            ghcr.io/${{ github.repository_owner }}/horas-discentes-frontend:latest

  backend:
    name: 🏗️ Backend CI/CD
    needs: prepare-tag
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        run: |
            cd Back
            cd Back.API
            dotnet restore Back.sln

      - name: Docker login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Backend Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./Back
          file: Back/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/horas-discentes-backend:${{ needs.prepare-tag.outputs.new_tag }}
            ghcr.io/${{ github.repository_owner }}/horas-discentes-backend:latest

  create-release:
    name: 🚀 Criar Release
    needs: [frontend, backend]
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    env:
      NEW_TAG: ${{ needs.prepare-tag.outputs.new_tag }}
      TIPO:    ${{ needs.prepare-tag.outputs.tipo }}
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Publicar release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ env.NEW_TAG }}
          name: Release ${{ env.NEW_TAG }}
          generate_release_notes: true
          body: |
            Release gerada automaticamente a partir da Pull Request #${{ github.event.pull_request.number }}
            Tipo de mudança: ${{ env.TIPO }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
